# 资料分析助理（DAA）— 架构规格

## Problem Statement

用户在做资料分析时，需求的复杂度差异很大：有时只是「读一份资料、算个统计、画张图」这种单纯任务；有时是「上传多份 CSV、边分析边发现观点、跳到另一份资料继续挖、最后从有意义的发现中下结论」这种探索式、没有固定顺序的复杂任务。用户希望有一个对话式的助理，能陪他们做这种开放式的资料探索，而不是被绑在一个预先设计好的固定流程里。

同时，因为分析过程会累积大量表格、统计结果、图表，用户需要能追溯「这个数字是怎么算出来的」，也希望分析状态能在换一个新对话、或隔一段时间回来之后，还能继续，而不是每次都要重新上传、重新做一次。

## Solution

用 LangGraph 建一个对话式 agent，背后用 DuckDB 管理每次对话累积的分析状态（表格、图表、描述、血缘）。分析本身（查询、统计、画图）交给 agent 自己生成 SQL/Python 代码执行，而不是做成一堆固定的操作函式，以符合"没有固定 workflow"的探索需求。所有会持久化的产出物都透过一个统一的登记机制（原子性写入），保证 metadata 系统跟实际资料不会不一致。当用户明确想要一份整理好的结论报告时，走一个"提案大纲 → 用户确认/修改 → 生成"的 human-in-the-loop 流程。分析状态有保留期限，过期会被清理，但用户原始上传的档案不受影响。

## User Stories

1. As a 用户, I want 在对话中任何时间点（一开始或中途）上传一份或多份 CSV, so that 我可以随时把新资料纳入正在进行的分析，不用重启对话。
2. As a 用户, I want 直接要求做简单的统计或画图, so that 我能快速看一眼资料长什么样，不用经过复杂的确认流程。
3. As a 用户, I want 在多份资料之间自由跳转、探索、发现观点再回头挖别的资料, so that 我可以做开放式、非线性的分析，不被固定流程绑住。
4. As a 用户, I want 助理记得这次对话已经产生过哪些表/图, so that 我不会在长时间的分析过程中失去脉络。
5. As a 用户, I want 每一张表/图产生时都自动附上一句话描述, so that 我不用自己手动帮每个中间结果写文件。
6. As a 用户, I want 每一个衍生出来的结果都记录"是从哪些表、用什么方法算出来的"（血缘）, so that 我之后能信任并回头稽核这个结论是怎么来的。
7. As a 用户, I want 随时问一句"目前进度如何"就能得到简短回应, so that 简单的确认不需要触发一个笨重的报告流程。
8. As a 用户, I want 明确要求"帮我整理一份报告", so that 助理会把有意义的发现（而不是每一个中间计算）组织成一份连贯的结论。
9. As a 用户, I want 在助理真正生成报告之前，先看到它打算怎么组织这份报告（大纲）, so that 我可以在正式生成之前，先看出方向不对就纠正它。
10. As a 用户, I want 对提案的报告大纲给出修改意见，让助理重新提案, so that 最终报告真的符合我要的东西。
11. As a 用户, I want 上传失败或档案本身有问题时，得到清楚具体的错误讯息, so that 我知道该怎么修正、重新指定档案。
12. As a 用户, I want 重新打开先前的对话时，看到当时累积的分析状态（表、图）还在, so that 我不用把之前的工作重做一次。
13. As a 用户, I want 从一个清单里选择、切换回过去的对话记录, so that 我可以回顾讨论内容或当时的结果。
14. As a 用户, I want 就算我的分析状态因为长时间没用而被清理掉, 也能确定我原始上传的档案完全没被动到, so that 我随时能重新开始分析，不会丢失原始资料。
15. As a 开发者, I want SQL 执行工具被限制成只能下唯读的 SELECT 查询, so that 就算 agent 出错或被误导，也不会写出会破坏资料的语句。
16. As a 开发者, I want Python 代码执行环境用权限受限的方式运作（未来可升级成沙盒）, so that 任意生成的代码不会危及主机环境。
17. As a 开发者, I want 表格/图表的登记动作是原子性的（全有或全无）, so that metadata 系统不会因为中途失败而跟实际资料对不上。
18. As a 开发者, I want 探索性（不留存）跟持久化（要留存）的代码执行是两个不同的工具, so that 不是每一次临时计算都会污染 metadata 登记系统。
19. As a 开发者, I want 依赖 LLM 的步骤（比如写描述）失败时能有限次数重试，而资料本身有问题（档案损毁等）时立刻回报明确错误、不重试, so that 系统在部分失败时的行为是可预期的。
20. As a 开发者, I want 一个新对话默认连到一份独立、全新的资料库（`thread_id` 与储存位置一对一）, so that 架构一开始保持简单，日后需要"多个对话共用同一份资料"时，可以用加一层对照表的方式扩充，不用重构。

## Implementation Decisions

### 储存结构

- 一个对话（`thread_id`）对应一份独立的 DuckDB 资料库，路径为 `user_id/thread_id/database.db`。这是刻意选择的一对一简化模型，不是最终定论——未来若要支援"多个对话共用同一份分析状态"，可以在这个结构外面加一层 `thread_id → workspace_id` 的对照表，不需要搬迁既有资料（因为现况下 `workspace_id` 本来就等于 `thread_id`）。
- 用户原始上传的 CSV 档案，独立存放在不受这份 `database.db` 生命週期管理影响的位置（不在会被清理排程整个砍掉的资料夹底下），确保清理动作不会波及原始资料。
- 分析过程产生的图表文件，存放在该对话资料夹底下（例如 `charts/` 子资料夹），DuckDB 的 metadata 只记录文件路径。

### Metadata 登记机制

所有会持久化的产出物（原始载入的表、分析衍生的表、图表）统一登记进同一张 metadata 表，概念上的 schema：

```
_meta_artifacts (
  name            TEXT,           -- 表名（kind='table' 时即 DuckDB 里真实的表名）或图表的登记名称
  kind            TEXT,           -- 'table' | 'chart'
  location        TEXT NULL,      -- kind='table' 时为 NULL（name 本身即可查询）；kind='chart' 时为文件路径
  description     TEXT,           -- 人类可读的语义描述
  parent_tables   LIST(TEXT),     -- 血缘：这个产出物是从哪些既有的 artifact 衍生的
  derivation      TEXT,           -- 产生这个 artifact 的 SQL 文字或代码
)
```

- 原始资料表与衍生表/图表走同一套登记逻辑，差别只在于"写描述所需的资讯来源"不同：原始表需要先看 schema/样本才能写描述；衍生表/图表在产生的当下，agent 已经具备写描述所需的完整上下文，不需要额外查看。

### 登记的原子性

- 「载入 CSV」被设计成单一原子工具：读档 → 内部生成描述（LLM 调用失败时有限次数重试，例如 2-3 次）→ 建表 + 写入 `_meta_artifacts`，全部包在同一个 DuckDB 交易（transaction）里，成功才 commit，任何一步失败就整个 rollback，不留下半成品。
- 档案本身的问题（编码错误、无法解析、空档案等）不重试，直接回报具体、可行动的错误讯息给用户，请其重新指定档案。
- 「分析结果落地」同样是一个独立的原子工具（区别于探索性执行工具），要求呼叫时同时带上 description、parent_tables、derivation 作为必要参数，执行代码 + 建表/存图 + 写入 metadata 一起在同一个交易里完成。
- 这个"原子交易"的设计，取代了原本考虑过的"用 schema 隔离或 status 栏位去过滤未完成产出物"的方案——因为原子性保证了根本不会存在"已建立但未登记"的中间态，不需要额外机制去隐藏它。

### 分析工具设计：生成代码，而非固定函式库

- 实际做分析的部分（查询、统计、画图）透过 agent 生成 SQL / Python 代码执行，不做成 `get_schema`、`select_column` 这类固定、穷举式的操作函式——因为分析需求本质上开放、无法穷举，固定函式库会跟"没有固定 workflow"的核心需求矛盾，也会导致工具数量膨胀、agent 选择工具的稳定度下降。
- 工具依风险分级，而非依语言分类：
  - `run_sql`：唯读，限制成只能下 `SELECT`（透过将底层 DuckDB 连线开成 read-only 模式来强制，而不是只靠 docstring/提示词约束），可自由、无限制地被 agent 调用
  - `run_python`（探索用）：执行任意代码但不涉及持久化，结果直接回传给 agent，用于临时计算、中途检查、试错，可自由调用
  - `run_python_and_register`（持久化用）：跟前者共用执行环境，但额外原子性地完成落地登记（见上一节）
- 判断「这次 Python 执行的结果值不值得登记」是语意层面的判断，只有 LLM 能做——graph/代码层不做这个判断，只负责在 agent 选择呼叫哪个工具之后，保证该工具的执行结果是可靠、一致的。这是整体设计贯穿的原则：**graph/代码负责结构与一致性保证（原子性、必要参数），LLM 负责语意与判断（重不重要、描述怎么写、该不该整理报告）**。
- 固定、简单的登记/簿记类工具（例如原始档案的 schema/样本预览）仍然保留，因为这类动作本身不是"分析"，是登记流程的一部分，适合做成固定、安全的操作。

### 执行环境

- Python 代码在权限受限的独立子进程中执行，不在主进程内直接 `exec`；不做完整容器化/沙盒隔离作为第一阶段目标（等到有多租户或需要处理不受信任资料的需求时再升级）。
- 子进程不持有 DuckDB 连线（避免与拥有正式连线的主进程发生写入冲突）。子进程若产生表格结果，先序列化成暂存 Parquet 档；若产生图表，直接写入该对话的 charts 资料夹。真正的 DuckDB 交易（读取暂存 Parquet 进正式表 + 写 metadata）由持有连线的主进程执行。
- 所有工具内部涉及的档案路径一律使用相对于该对话工作资料夹的相对路径，不使用绝对主机路径，确保未来把执行环境换成容器/沙盒时不需要更动逻辑。

### 结论 / 报告收敛流程

- 「小结」（用户只是要一个图或一个统计结果时，agent 顺带给的摘要）不需要额外机制，就是 agent 正常回应的一部分。
- 「整理报告」这个较重的流程，只在 agent 判断用户确实有此意图时触发（依语意判断，而非关键字比对），避免为单纯的探索性请求增加不必要的确认负担。
- 触发后走 human-in-the-loop 流程：agent 查询 `_meta_artifacts`，草拟报告大纲（打算用哪些产出物、打算怎么组织叙事结构、哪个发现是重点），透过 LangGraph 的 `interrupt()` 暂停等待用户确认；用户批准则往下走生成最终报告，用户提出修改意见则带着新指示回到草拟大纲的步骤重新提案，如此循环，设一个软性的轮数上限（例如 3-5 轮），超过后 agent 应改变策略（例如请用户直接列出明确要用哪些资料/顺序），而不是无限重试。

### Graph 结构

- 主体是标准的 agent + tools 循环（ReAct 模式），所有分析/登记/探索工具都挂在同一个 agent node 上，不需要为每个工具各自开 node。
- 唯一需要额外 graph 结构（分岔、循环）的地方是「整理报告」的提案确认流程，使用 LangGraph 的 `interrupt()` / `Command` 搭出一个会暂停、会根据用户回应分岔的子图。

### 对话与储存的连接

- 前端只需要认识 `thread_id`：选择/建立一个 `thread_id`，放进 LangGraph 的 `config`，正常呼叫 `graph.invoke()` / `.stream()`，不需要额外处理储存层的复杂度。
- 现阶段 `thread_id` 与储存路径一对一（见「储存结构」一节），因此不需要额外的对照表；这是刻意的简化，保留了未来加上多对一对照的扩充空间。

### 生命週期管理

- 一个外部的定期排程扫描各对话的资料夹，删除超过保留期限（例如一段时间没被存取）的 `database.db` 及其衍生的图表/暂存文件；用户原始上传的 CSV 因为存放在独立位置，不受影响。
- 工具在尝试开启一份已经被清理掉的资料库时，需要侦测到这个状况（档案不存在），回报清楚的讯息告知用户「分析状态已过期，但原始档案安全，请重新开始」，而不是让原始的文件系统错误直接抛出或让程式崩溃。

## Testing Decisions

目前专案仍在架构规划阶段（现有代码仅有骨架 `main.py`、空的 `mcps.py`），尚未进入实作、也还没有具体讨论测试细节。可以先确立的原则，留待实作阶段展开：

- 优先测试外部行为，而非实作细节——例如「原子登记工具在执行过程中失败时，DuckDB 里不应留下任何新表或新的 metadata 记录」这种可观察的结果，而不是测试内部有没有开 transaction。
- 涉及原子性/rollback 的工具（`load_csv`、`run_python_and_register`）需要针对"中途失败"路径专门测试，确保失败后系统状态与执行前一致。
- `run_sql` 的唯读限制需要有测试验证：即使传入 `DROP`/`DELETE` 等语句，也会被资料库层级拒绝，而不是仅靠没被触发的假设。
- 目前专案没有既有的测试先例可参照，测试框架与惯例待实作阶段建立。

## Out of Scope

- 完整的容器化/沙盒执行环境——现阶段用权限受限的子进程代替，但工具介面设计上已保留了替换空间，留待有多租户或不受信任资料的需求时再做。
- `thread_id` 与储存位置解耦（多个对话共用同一份资料/workspace）——已确认是合理且有价值的未来需求，但刻意排除在这一版之外，先用一对一的简单模型。
- `run_sql` 唯读限制的具体强制机制（SQL parser 或 DuckDB 权限设定的选型）——已确定方向（要用资料库层级强制，而不是靠 docstring），但尚未选定具体作法。
- 报告最终产出的档案格式（Markdown / HTML / PDF）——不影响架构，留到后面再定。
- 前端的 workspace/对话选择 UI 细节——只确认了「需要一个对话清单让用户选」这个需求，具体呈现方式未讨论。
- 多个使用者/多个对话同时并发存取同一份资料库的情境——已识别为边缘情况，目前的一对一模型天然降低了这个风险，但尚未深入设计。
- 原始 CSV 档案的确切存放路径——已确认原则（独立存放、不受 `database.db` 清理排程影响），但具体路径规划是在还有 `workspace_id` 概念时讨论的，简化成 `user_id/thread_id/database.db` 这个一对一结构之后，尚未回头重新确认 CSV 应该放在哪。

## Further Notes

这份 spec 是透过一系列深入的架构讨论逐步收敛出来的，过程中修正过几次重要的设计假设（例如：一开始误以为 `database.db` 是需要长期保存的正式状态，后来厘清它其实有保留期限，用户原始 CSV 才是真正的 source of truth；也一度设计了 `workspace_id` 与 `thread_id` 解耦的机制，后来确认目前不需要，退回更简单的一对一模型，但保留了未来扩充的路径）。

有一个贯穿整个设计的原则，值得在实作阶段持续参照：**graph/代码层负责保证结构与一致性（原子性、必要参数、唯读限制这类"不能出错"的保证），LLM 负责语意与判断（这个结果重不重要、描述该怎么写、该不该触发报告流程这类"需要理解上下文"的决定）。** 遇到新的设计问题时，可以先问"这是结构问题还是判断问题"，往往能很快定出该由谁负责。
