# 進度日誌

## 目前架構

`main.py` 是單一 agent（`create_agent`），用 `ChatOpenAI`（`google/gemma-4-31b-it`，經 OpenRouter），`model_kwargs={"parallel_tool_calls": False}` 讓 model 一次只發一個 tool call。用 `InMemorySaver` 做 checkpointer，支援多輪對話與 `interrupt()` 中斷續傳。

工具分成兩個檔案：
- `tools/default_tools.py`：資料庫/檔案/輸出相關工具
- `tools/code_tools.py`：Python 程式碼執行相關工具（尚未接進 `main.py`）

## 已完成並接上 main.py 的工具（`tools/default_tools.py`，9 個）

- `is_file_exist(file_path)` -> bool
- `create_database(database_path)` -> bool
- `is_table_exist(database_path, table_name)` -> bool（新增，尚未加進 `main.py` 的 tools 清單，也還沒討論過）
- `create_table(database_path, table_name, table_path)` -> bool
- `is_columns_exist(database_path, table_name, column_names)` -> dict
- `search_column_type_distribution_with_pattern(database_path, table_name, column_pattern, pattern_mode)` -> dict（`pattern_mode` 用 `Literal['equals','contains','starts','ends']`，靠 enum 限制讓錯誤在 schema 驗證階段就被擋下）
- `select_columns(database_path, table_name, column_names, checkpoint_name)` -> dict
- `preview_table(database_path, table_name)` -> str（原本是 dict，因為 dict 包多行字串會被 `json.dumps()` 轉譯成看得到的 `\n`，改成純字串回傳解決）
- `export_table(database_path, table_name, table_path)` -> bool
- `show_image(image_path)` -> bool（用 `webbrowser.open(file_uri)` 開圖，不用 `platform.system()` 分平台，`webbrowser` 標準庫自己處理跨平台）
- `send_email(to_user, subject, body, attachment_path)` -> bool（Gmail SMTP + App Password，寄出前用 `interrupt()` 卡一個人工確認關卡，需回覆 `yes` 才會真的寄出；已用假 SMTP 測過 pause/resume 流程正常）

## `main.py` 額外機制

- `handle_tool_errors`：`wrap_tool_call` middleware，統一接住所有工具執行期間的例外，轉成錯誤訊息回傳給 model，而不是讓 `agent.invoke()` 崩潰。`create_agent` 預設只擋參數格式錯誤，不擋執行期例外，這個 middleware 補上這一塊。
- CLI 迴圈支援偵測 `__interrupt__`，印出確認訊息、用 `input()` 拿人類回覆、再用 `Command(resume=...)` 續跑。

## 已設計、還沒寫進 `tools/code_tools.py` 的工具

- `find_python_variable(variable_pattern, pattern_mode)`：跟 `preview_python_variables` 搭配的 grep 風格查詢，還沒動手寫

## `tools/code_tools.py`（尚未接進 `main.py`）

- `_python_variables = {}`：模組層級共用 dict，當 `exec()` 的 globals，讓多次 `run_python_code` 呼叫共用同一份變數空間（像同一個 Jupyter kernel）
- `run_python_code(code)` -> str：執行程式碼、截 stdout 回傳。目前沒有自己的 try/except，依賴 `main.py` 的 `handle_tool_errors` middleware 接住例外（代價：出錯時會拿不到 crash 前已印出的部分 stdout）
- `preview_python_variables()` -> dict：回傳 `{"variables": {...最多12個...}, "total": 實際總數}`，濾掉 dunder key 跟 import 的模組本身
- `_summarize(value)`：依型別給精簡摘要（shape / len / repr 頭尾截斷），參考了 IPython `%whos` 的做法（換行轉義、頭尾各留 25 字 + `<...>`、`repr()` 包 try/except 防止壞掉的物件炸掉）。中途討論過要不要簡化成只留型別名稱，最後結論是保留現在的版本——因為 model 寫程式碼「之前」就需要知道 shape/len 這些資訊才能少犯錯，不是單純的方便功能

## 待辦 / 尚未實作

1. `find_python_variable(variable_pattern, pattern_mode)` — grep 風格查詢單一/少數變數
2. 把 `run_python_code`、`preview_python_variables`、`find_python_variable` 接進 `main.py` 的 `tools` 清單
3. 畫圖流程：agent 生成程式碼 `plt.savefig(path)` → 呼叫已存在的 `show_image(path)` 開圖，這條路徑還沒實際跑過
4. 工具數量已到 9-10 個（接上 code_tools 後會到 12+），還在安全範圍（研究顯示 15-20 個開始準確率下降），但已經找出未來拆分方向：「資料庫操作」vs「對外輸出/通知」兩群，工具再變多時可考慮拆成 supervisor + sub-agent
5. 之後要接公司內網 mail relay（不同於 Gmail 的 SMTP 設定，可能不需要帳密認證），現在的 `send_email` 先只支援 Gmail，SMTP 設定目前是寫死的（`smtp.gmail.com:587`），之後要改成可配置

## 發現但還沒處理的小狀況（值得下次確認）

- `preview_table` 目前的 SQL 是 `SELECT * FROM {table_name}`（沒有 `LIMIT`），會把整張表讀進記憶體再用 `to_string(max_rows=6, ...)` 截斷顯示——資料表很大的話這裡會不必要地耗記憶體，值得確認是否要加回 `LIMIT`
- `preview_table` 裡還留著一段註解掉的舊 dict 版本回傳（第 161-165 行），確認不會用到的話可以刪掉
- `main.py` 的 `agent.invoke()` 沒有設定 `recursion_limit`，目前 langgraph 預設值是 10007，對「model 一直修不好、無限重試」這種情況防護力不夠，建議之後明確設一個低一點的數字（例如 20）
- `is_table_exist` 這個工具是新加的，還沒討論過用途、也還沒加進 `main.py` 的 tools 清單

## 這次 session 學到、後續要延續的原則

- 不要為了驗證一個函式的結果去呼叫另一個函式重新確認，如果前面的操作本身失敗就會丟例外的話（已存進記憶 `feedback_no_redundant_coupling`）
- Tool 回傳值如果是 dict、又包含多行字串，會被 `json.dumps()` 轉譯壞掉，多行內容建議直接回傳 `str`
- 工具參數盡量扁平（單層 dict 可接受，避免 tuple/list[dict] 這類深層巢狀），`Literal` enum 比自由字串更安全
- 錯誤處理統一寫在 `wrap_tool_call` middleware，不要每個工具各自重複 try/except
