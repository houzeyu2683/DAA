# 思考：preview_python_variables 這條路是不是走偏了？

## 問題意識

我們花了很多輪在設計 `preview_python_variables`/`_summarize`：先是型別名稱，覺得不夠 → 加 shape/len → 覺得還不夠 → 又想加 DataFrame 的欄位名稱……每次補一個資訊，都會發現「還不夠」，這個清單感覺沒有終點。這是不是代表方向本身有問題？

## 查到的主流做法

查了 AutoGen 的 Jupyter Code Executor（目前這類「持久 kernel 執行程式碼」agent 框架裡最常被引用的實作之一）：

> AutoGen 的 Jupyter code executor 靠的是「在同一個 kernel 裡持久執行」這個架構本身，**沒有任何自動的「列變數」或 namespace 摘要功能**。狀態感知完全靠：
> 1. Agent 記得自己之前執行過的程式碼、印出過的結果（都在對話歷史裡）
> 2. System prompt 明確指示：「利用 kernel 的持久性，逐步累加寫程式碼，不要重複造輪子」

也就是說，「知道 session 裡有什麼」這件事，主流做法根本不靠一個額外工具去查，而是：
- Agent 自己記得（它就是那個寫程式碼、印出結果的人，天生就知道自己做過什麼）
- 真的忘記或不確定的時候，直接**再寫一段程式碼 `print()` 出來看**，不是靠一個專門的「摘要工具」預先幫它準備好

更廣泛的 context engineering 研究也呼應這點：好的做法是「狀態存在 prompt 之外，只在真正需要的當下才把相關的部分拉進來」，而不是每次都把整個環境的摘要硬塞進上下文。

## 反思

我們這幾輪的討論，其實是在幫 `preview_python_variables` 疊加越來越多資訊（型別 → shape/len → 想加欄位名稱），試圖讓 agent「不用問就先知道一切」。但主流做法顯示這個方向本身可能就不太對——真正被驗證過的做法是反過來：**讓 agent 自己決定要看什麼，用 `run_python_code` 寫 `print()` 去問，而不是設計一個工具試圖預測它需要看什麼**。

這也解釋了為什麼這個清單感覺「永遠不夠」：因為我們在用一個固定的摘要工具，去猜測所有可能情境下 agent 需要的所有資訊，這本質上是猜不完的——今天覺得要加 shape，明天可能又要加 dtype，後天可能要加欄位的 null 比例。反觀「agent 自己 print()」這條路，不管它想看什麼，都是同一招搞定，不需要我們預先窮舉。

## 這對我們現有設計的意義

- `preview_python_variables`（列出目前有哪些變數、精簡摘要）本身可能還是有價值——用在「對話被壓縮/摘要過、舊的上下文丟失」或「檢查有沒有忘記清掉的東西」這種救急情境
- 但不需要為了讓它做到「agent 完全不用再問就能寫對程式碼」而不斷加欄位、加 dtype、加各種細節——這件事本來就該交給「agent 自己寫 `print()` 去問」解決，不是這個工具的責任
- `_summarize` 目前的版本（型別 + shape/len + 截斷過的 repr）已經足夠當「輕量摘要」，不需要再往下加東西；如果之後真的常常發現 agent 猜錯欄位名稱，比較對的解法可能是在 `run_python_code` 的 docstring / system prompt 裡提醒它「不確定欄位名稱時，先 print(df.columns) 確認」，而不是繼續往 `_summarize` 裡塞更多資訊

## 待確認

這只是研究結論、還沒實際改動任何程式碼。要不要：
1. 維持 `_summarize` 現狀（型別+shape/len），不再往裡面加欄位名稱
2. 在 `run_python_code` 的 docstring 加一句提醒（不確定的東西先 print 確認），取代原本想加進 `_summarize` 的欄位名稱功能
