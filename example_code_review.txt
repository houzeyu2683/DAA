example_code.py 概覽

| 區段 | 內容說明 |
|------|----------|
| **檔案說明（前置說明文字）** | 這是一個用來手動測試 tools/system.py 各種工具的範例檔案，裡面列出了一些可以直接在 REPL 中呼叫的範例指令（如 preview_files("."), search_files(".", "example_code.py"), read_file("example_code.py", 10, 20) 等）。 |
| **匯入** | import time – 只在 slow_task 中使用。 |
| **算術函式** | - add(a, b)、subtract(a, b)、multiply(a, b)、divide(a, b)、power(a, b)、modulo(a, b)、floor_divide(a, b) 每個函式都有 # TODO 註解，提醒要加入型別檢查、除以零例外處理、負指數處理等。 |
| **Cache 類別** | - Cache、Cache2、Cache3 三個簡易快取類別，皆以字典 _store 保存鍵值。每個類別都有 get（含 # TODO: 加上 miss 的 log）與 set（Cache 內有 # TODO: 加上容量上限）方法。 |
| **工作任務** | - slow_task()：使用 time.sleep(1)，註解指出未來要改成非同步版本。 - fast_task()：直接回傳字串 "done"。 |
| **main()** | 依序呼叫所有算術函式並印出結果，接著示範 Cache 的使用：\n```python\ncache = Cache()\ncache.set(\"x\", 1)\nprint(cache.get(\"x\"))\n``` |
| **入口點** | if __name__ == "__main__": → 執行 main()。 |

### 主要目的
- **示範**：提供一個可供 tools/system.py（如檔案預覽、搜尋、分頁讀取、程式碼搜尋）測試的樣本檔案。
- **練習**：裡面的 # TODO 註解可作為練習題目，讓開發者補足型別檢查、錯誤處理、日誌、容量限制或非同步改寫等功能。

### 簡單執行結果（python example_code.py）
```
3
2
6
3.0
1024
1
3
1
1
```
（最後的 1 來自 Cache 的 get("x")，因為先前 set("x", 1)）

如果你想要針對某個區塊進一步說明、修改或加入實作（例如完成 # TODO），隨時告訴我！